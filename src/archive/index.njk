---
title: archive
layout: home.njk
---

<div class="title">
    <h1>archive</h1>
    <h3>
        <p>共有 {{collections.posts.length}} 篇文章</p>
    </h>
</div>

{% for year in collections.archive %}
{% for items in year %}
<div>
    <a href="./{{items.year}}#{{ items.month }}">
        <div class="title">
            <h3 title="{{ items.year }}_{{ items.month }}">{{ items.year }}年{{ items.month }}月</h3>
            <div>共有 {{ items.length }} 篇</div>
        </div>
    </a>
    <ul>
        <li>
            {% for post in items %}
                <a href="{{ post.page.url }}">
                    <div style="display: flex;justify-content: space-between;" title="{{ post.data.description}}">
                        <span>{{ post.data.title}}</span>
                        <span> {{ post.date.getFullYear() }}-{{ post.date.getMonth() + 1 }}-{{ post.date.getDate() }}</span>
                    </div>
                </a>
            {% endfor %}
        </li>
    </ul>
</div>
{% endfor %}
{% endfor %}

<style>

</style>

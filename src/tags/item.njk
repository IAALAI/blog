---
layout: home.njk
pagination:
    data: collections.tags
    size: 1
    alias: tag
    addAllPagesToCollections: true
permalink: "/tags/{{tag.key}}.html"
eleventyComputed:
  title: "tag for: {{tag.key}}"
---
<div style="display: none;">
单独某一个 tag 的信息
</div>

<h2> {{ tag.key }} </h2>

<ul class="loopColor">
    {% for item in collections[tag.key] %}
        <li>
            <a href="{{item.data.page.url}}">{{item.data.title}}</a>
        </li>
    {% endfor %}
</ul>
